# Use root/example as user/password credentials
version: '3.1'

services:

  db:
    image: mysql
    container_name: mysql
    restart: always
    ports:
      - "3306:3306"
      - "33060:33006"
    environment:
      MYSQL_ROOT_PASSWORD: example
